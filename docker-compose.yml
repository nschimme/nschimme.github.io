services:
  jekyll:
    image: jekyll/jekyll:latest
    command: bash -lc "bundle install && bundle exec jekyll serve --host 0.0.0.0 --watch --force_polling --verbose"
    ports:
      - 4000:4000
    volumes:
      - .:/srv/jekyll
      - bundle_cache:/usr/local/bundle

volumes:
  bundle_cache:
